# HTML演示文稿制作与PDF导出 - 标准操作流程 (SOP)

> 基于 Reveal Presentation Core 框架的通用指南
>
> **目标**：使用模块化架构快速创建高质量HTML演示文稿，并完美导出为PDF/PPT

---

## 📋 目录

0. [快速开始](#0-快速开始)
1. [架构设计](#1-架构设计)
2. [技术栈选择](#2-技术栈选择)
3. [项目结构](#3-项目结构)
4. [配色方案设计](#4-配色方案设计)
5. [HTML核心配置](#5-html核心配置)
6. [CSS样式设计原则](#6-css样式设计原则)
7. [防止内容溢出](#7-防止内容溢出的设计规范)
8. [PDF导出配置](#8-pdf导出配置)
9. [PPT转换工具](#9-ppt转换工具)
10. [质量检查清单](#10-质量检查清单)
11. [常见问题](#11-常见问题与解决方案)
12. [最佳实践](#12-最佳实践总结)
13. [工具推荐](#13-工具推荐)

---

## 0. 快速开始

### 0.1 从模板创建新演示

```bash
# 1. 进入工作目录
cd /path/to/your/presentations

# 2. 从核心模板创建新项目
cp -r reveal-presentation-core/templates MyPresentation
cd MyPresentation

# 3. 创建幻灯片目录
mkdir -p slides presentation_images

# 4. 创建 manifest.json
cat > slides/manifest.json << 'EOF'
[
  {"file": "01_title.html"},
  {"file": "02_content.html"}
]
EOF

# 5. 创建标题幻灯片
cat > slides/01_title.html << 'EOF'
<section class="title-slide">
  <h1>我的演示</h1>
  <p class="subtitle">精简有力的副标题</p>
  <p class="authors">作者名</p>
  <p class="affiliation">机构名</p>
</section>
EOF

# 6. 创建内容幻灯片
cat > slides/02_content.html << 'EOF'
<section>
  <h2>幻灯片标题</h2>
  <ul>
    <li>要点一</li>
    <li>要点二</li>
  </ul>
  <div class="insight-box">
    <strong>核心观点：</strong>这是一个洞察框
  </div>
</section>
EOF

# 7. 预览
python3 -m http.server 8000
# 访问 http://localhost:8000/index.html
```

### 0.2 导出为 PDF

```bash
# 使用项目提供的导出脚本
./export_pdf.sh

# 输出位置：out/index.pdf
```

### 0.3 转换为 PPT

```bash
# 安装 Python 依赖（仅首次）
pip install -r ../reveal-presentation-core/tools/requirements_ppt.txt

# 转换 PDF 为 PPT
python3 ../reveal-presentation-core/tools/pdf_to_ppt.py out/index.pdf out/presentation.pptx
```

### 0.4 预定义样式类

使用核心框架提供的样式类（无需写 CSS）：

**特殊幻灯片**：
- `.title-slide` - 标题页（渐变背景）
- `.section-divider` - 章节分隔页（居中对齐）
- `.dense-slide` - 高密度内容页（缩小字体）
- `.ultra-dense-slide` - 超高密度内容页（进一步缩小）
- `.spacious-slide` - 宽松布局页（更大间距）

**内容盒子**：
- `.observation-box` - 观察/问题（红色边框）
- `.insight-box` - 洞察/分析（蓝色边框）
- `.solution-box` - 解决方案（绿色边框）
- `.formula-box` - 公式展示（灰色背景）
- `.highlight` - 高亮文本（黄色背景）

**布局**：
- `.two-columns` - 两列布局
- `.three-columns` - 三列布局
- `.comparison-box` - 对比布局
  - `.comparison-left` - 左侧（红色边框）
  - `.comparison-right` - 右侧（绿色边框）
- `.logo-grid` - Logo 网格布局
- `.logo-item` - Logo 项

**列表**：
- `.compact-list` - 紧凑列表（减小行间距）

**工具类**：
- `.checkmark` - 绿色对勾（✓）
- `.crossmark` - 红色叉号（✗）
- `.image-caption` - 图片说明

---

## 1. 架构设计

### 1.1 设计哲学

> **"关注分离，模块复用，核心优先"**

本框架采用三层架构，实现样式、逻辑与内容的完全解耦。

### 1.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    演示层 (Projects)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Project A    │  │ Project B    │  │ Project C    │ ...  │
│  │ (仅包含内容)  │  │ (仅包含内容)  │  │ (仅包含内容)  │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │              │
│         └──────────────────┼──────────────────┘              │
│                            ↓                                 │
│                       import core                            │
└─────────────────────────────────────────────────────────────┘
                             │
┌────────────────────────────┼─────────────────────────────────┐
│           核心层 (reveal-presentation-core)                   │
│                            │                                 │
│    ┌───────────────────────┼───────────────────────────┐    │
│    │  样式系统                                          │    │
│    │  presentation-base.css                            │    │
│    │  • CSS 变量主题系统                                │    │
│    │  • 语义化样式类 (.insight-box, .two-columns...)   │    │
│    │  • 打印/PDF 优化 (@media print)                   │    │
│    │  • 密度控制 (.dense-slide, .ultra-dense-slide)   │    │
│    └────────────────────────────────────────────────────┘    │
│                                                               │
│    ┌────────────────────────────────────────────────────┐    │
│    │  逻辑系统                                          │    │
│    │  presentation-loader.js                           │    │
│    │  • 异步加载 manifest.json                         │    │
│    │  • 并发加载幻灯片 HTML                            │    │
│    │  • DOM 注入与错误处理                             │    │
│    │  • Reveal.js 初始化                               │    │
│    └────────────────────────────────────────────────────┘    │
│                                                               │
│    ┌────────────────────────────────────────────────────┐    │
│    │  工具系统                                          │    │
│    │  tools/                                           │    │
│    │  • pdf_to_ppt.py - PDF→PPT 转换（支持参数）       │    │
│    │  • overflow-detector/ - 内容溢出检测              │    │
│    │  • generate-pdf-custom.js - 自定义PDF生成         │    │
│    └────────────────────────────────────────────────────┘    │
│                                                               │
│    ┌────────────────────────────────────────────────────┐    │
│    │  模板系统                                          │    │
│    │  templates/index.html                             │    │
│    │  • HTML 结构模板                                   │    │
│    │  • CDN 资源引用                                    │    │
│    └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                             │
┌────────────────────────────┼─────────────────────────────────┐
│                   基础层 (Infrastructure)                     │
│    ┌────────────────────────────────────────────────────┐    │
│    │  第三方库 (CDN)                                    │    │
│    │  • Reveal.js 4.6.0                                 │    │
│    │  • MathJax 3                                       │    │
│    │  • Reveal.js Plugins (Notes, Highlight, Math)     │    │
│    └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 核心原则

**1. Core-First（核心优先）**
- ✅ 所有样式在 `reveal-presentation-core/css/presentation-base.css`
- ✅ 所有工具在 `reveal-presentation-core/tools/`
- ✅ 项目目录**零 CSS**、**零 JS**，只包含内容
- ❌ 不要在项目中复制核心样式

**2. Content-Only（内容纯粹）**
- 项目目录只包含：`slides/`、`presentation_images/`、`index.html`、`export_pdf.sh`
- 所有通用逻辑和样式由核心提供

**3. Single Source of Truth（单一真相源）**
- 核心框架是所有项目的唯一样式来源
- 任何样式修改在核心进行，所有项目自动受益

### 1.4 数据流

#### 页面加载流程

```
用户访问 index.html
    ↓
加载 presentation-base.css (样式)
    ↓
加载 presentation-loader.js (逻辑)
    ↓
执行 initPresentation()
    ↓
异步加载 slides/manifest.json
    ↓
并发加载所有幻灯片 HTML
    ↓
注入到 DOM (#slides-root)
    ↓
初始化 Reveal.js
    ↓
渲染完成
```

#### PDF 导出流程

```
运行 export_pdf.sh
    ↓
启动 HTTP 服务器 (如未运行)
    ↓
调用 Chrome Headless
    ↓
加载 HTML + ?print-pdf 参数
    ↓
等待幻灯片加载完成
    ↓
打印为 PDF
    ↓
保存到 out/index.pdf
```

---

## 2. 技术栈选择

### 2.1 核心框架

```javascript
// 推荐技术栈
{
  "presentation": "Reveal.js 4.6.0",
  "core": "Reveal Presentation Core (自研框架)",
  "math": "MathJax 3",
  "export": "Chrome Headless",
  "structure": "Modular (模块化幻灯片)"
}
```

### 2.2 为什么选择 Reveal.js？

✅ **优势**：
- 原生支持PDF导出（`?print-pdf` 模式）
- 丰富的布局选项（两栏、网格等）
- 数学公式支持（MathJax/KaTeX）
- 响应式设计
- 代码高亮
- 活跃的社区支持

⚠️ **注意事项**：
- 需要配置 `pdfMaxPagesPerSlide: 1` 防止分页
- 打印CSS需要单独优化
- 图片高度需严格控制

### 2.3 为什么选择模块化架构？

✅ **优势**：
- **易于维护**：每个幻灯片独立文件
- **灵活排序**：修改 manifest.json 即可调整顺序
- **团队协作**：多人可同时编辑不同幻灯片
- **版本控制友好**：Git 可清晰追踪每页变更
- **复用性强**：样式、工具完全共享

---

## 3. 项目结构

### 3.1 推荐目录结构

```
MyPresentation/                 # 项目目录（仅包含内容）
├── index.html                  # HTML入口（import core）
├── export_pdf.sh               # PDF导出脚本
├── slides/                     # 幻灯片内容
│   ├── manifest.json           # 幻灯片清单
│   ├── 01_title.html           # 标题页
│   ├── 02_intro.html           # 介绍页
│   ├── 03_content.html         # 内容页
│   └── ...
├── presentation_images/        # 图片资源
│   ├── diagram1.png
│   └── chart.png
├── out/                        # 输出目录
│   ├── index.pdf               # 导出的PDF
│   └── presentation.pptx       # 转换的PPT
├── package.json                # npm依赖配置（可选）
└── README.md                   # 项目说明（可选）
```

**核心框架位置**：`../reveal-presentation-core/`

```
reveal-presentation-core/       # 通用框架（所有项目共享）
├── css/
│   └── presentation-base.css   # 核心样式（所有样式都在这里）
├── js/
│   └── presentation-loader.js  # 加载器
├── tools/                      # 通用工具
│   ├── pdf_to_ppt.py           # PDF→PPT 转换
│   ├── requirements_ppt.txt    # Python 依赖
│   ├── overflow-detector/      # 溢出检测工具
│   └── generate-pdf-custom.js  # 自定义PDF生成
└── templates/                  # 模板文件
    └── index.html              # HTML 模板
```

### 3.2 index.html 模板

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>我的演示文稿</title>

  <!-- Reveal.js 核心 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/dist/reset.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/dist/reveal.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/dist/theme/white.css">

  <!-- MathJax -->
  <script src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>

  <!-- 核心样式（单一真相源） -->
  <link rel="stylesheet" href="../reveal-presentation-core/css/presentation-base.css">
</head>
<body>
  <div class="reveal">
    <div class="slides" id="slides-root"></div>
  </div>

  <!-- Reveal.js 插件 -->
  <script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/dist/reveal.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/plugin/notes/notes.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/plugin/markdown/markdown.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/plugin/highlight/highlight.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.0/plugin/math/math.js"></script>

  <!-- 核心加载器 -->
  <script src="../reveal-presentation-core/js/presentation-loader.js"></script>

  <!-- 初始化 -->
  <script>
    initPresentation({
      reveal: {
        width: 1920,    // Full HD
        height: 1080,   // 16:9
        margin: 0.08    // 8% 边距
      }
    });
  </script>
</body>
</html>
```

### 3.3 manifest.json 格式

```json
[
  {"file": "01_title.html"},
  {"file": "02_intro.html"},
  {"file": "03_method.html"},
  {"file": "04_results.html"}
]
```

**命名规范**：
- 使用数字前缀确保顺序：`01_`, `02_`, `03_`...
- 避免使用字母后缀：❌ `13a.html`, `13b.html`
- 拆分页面在标题中标注：✅ `标题（1/2）`、`标题（2/2）`

---

## 4. 配色方案设计

### 4.1 核心色彩系统（已在 core 中定义）

核心框架提供两套变量别名，完全兼容：

```css
:root {
  /* 主色调 */
  --primary-dark: #303A52;        /* 深蓝 */
  --primary-blue: #4A90E2;        /* 科技蓝 */
  --primary-accent: #F9AA33;      /* 强调金 */

  /* 语义色 */
  --text-primary: #333333;        /* 正文 */
  --text-light: #666666;          /* 次要文字 */
  --bg-light: #F5F5F5;            /* 背景 */
  --success: #2ECC71;             /* 成功/通过 */
  --warning: #E74C3C;             /* 警告/问题 */
  --info: #4A90E2;                /* 信息 */

  /* 兼容别名（与旧项目兼容） */
  --color-deep-blue: var(--primary-dark);
  --color-tech-blue: var(--primary-blue);
  --color-accent-gold: var(--primary-accent);
  --color-text-gray: var(--text-primary);
  --color-light-gray: var(--bg-light);
  --color-success-green: var(--success);
  --color-warning-red: var(--warning);
}
```

### 4.2 配色使用规范

| 元素类型 | CSS变量 | 颜色值 | 用途 |
|---------|---------|--------|------|
| **主标题** | `--primary-dark` | #303A52 | H1 |
| **副标题** | `--primary-blue` | #4A90E2 | H2/H3 |
| **强调** | `--primary-accent` | #F9AA33 | `.highlight` |
| **正文** | `--text-primary` | #333333 | 段落 |
| **成功** | `--success` | #2ECC71 | `.solution-box` |
| **问题** | `--warning` | #E74C3C | `.observation-box` |
| **信息** | `--info` | #4A90E2 | `.insight-box` |
| **背景** | `--bg-light` | #F5F5F5 | 盒子背景 |

### 4.3 自定义主题（可选）

如需自定义颜色，创建 `css/custom.css`：

```css
:root {
  /* 覆盖核心变量 */
  --primary-dark: #your-color;
  --primary-blue: #your-color;
  --primary-accent: #your-color;
}
```

在 `index.html` 中引入（在核心样式**之后**）：

```html
<link rel="stylesheet" href="../reveal-presentation-core/css/presentation-base.css">
<link rel="stylesheet" href="css/custom.css">
```

---

## 5. HTML核心配置

### 5.1 Reveal.js 初始化配置

核心框架的 `presentation-loader.js` 提供默认配置，可通过 `initPresentation()` 覆盖：

```javascript
initPresentation({
  reveal: {
    // ===== 基本配置 =====
    hash: true,                     // URL 哈希导航
    slideNumber: 'c/t',             // 显示页码 (当前/总数)

    // ===== PDF导出关键配置 =====
    width: 1920,                    // Full HD宽度
    height: 1080,                   // Full HD高度
    margin: 0.08,                   // 8%边距（推荐值）
    pdfMaxPagesPerSlide: 1,         // 🔥 每张幻灯片最多1页PDF
    pdfSeparateFragments: false,    // 🔥 不为动画创建额外页

    // ===== 缩放范围 =====
    minScale: 0.2,
    maxScale: 2.0,

    // ===== 动画效果 =====
    transition: 'slide',            // slide | fade | convex | concave | zoom
    transitionSpeed: 'default',     // default | fast | slow
    backgroundTransition: 'fade',

    // ===== 插件（已自动配置） =====
    plugins: [
      RevealMarkdown,
      RevealHighlight,
      RevealNotes,
      RevealMath.KaTeX
    ]
  }
});
```

### 5.2 关键参数说明

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| **width × height** | 1920 × 1080 | Full HD，16:9比例 |
| **margin** | 0.08 (8%) | 防止边缘裁切，影响内容显示大小 |
| **pdfMaxPagesPerSlide** | 1 | **必须**，防止单张幻灯片分成多页PDF |
| **pdfSeparateFragments** | false | **必须**，防止动画分页 |

---

## 6. CSS样式设计原则

### 6.0 核心样式优先（Core-First）⭐

**最重要的原则**：

✅ **DO（正确做法）**：
- 使用核心提供的样式类：`.two-columns`、`.insight-box`、`.dense-slide` 等
- 直接 import 核心样式，项目**零 CSS**
- 所有通用样式改进在核心进行

❌ **DON'T（错误做法）**：
- 在项目中复制核心样式
- 在项目中创建 `css/style.css`（除非有**真正**项目特定的样式需求）
- 覆盖核心的打印规则

**引入顺序**：

```html
<!-- 只需这一行！ -->
<link rel="stylesheet" href="../reveal-presentation-core/css/presentation-base.css">
```

### 6.1 核心提供的样式类

核心框架已提供完整的样式系统，**开箱即用**：

**特殊幻灯片**：
- `.title-slide` - 标题页（蓝色渐变背景，白色文字）
- `.section-divider` - 章节分隔页（flex 居中）
- `.dense-slide` - 高密度内容（padding: 0.5em 0.9em）
- `.ultra-dense-slide` - 超高密度（padding: 0.3em 0.6em）
- `.spacious-slide` - 宽松布局（padding: 0.6em 1.2em）

**内容盒子**：
- `.observation-box` - 红色边框，渐变背景（问题/观察）
- `.insight-box` - 蓝色边框，渐变背景（洞察/分析）
- `.solution-box` - 绿色边框，渐变背景（解决方案）
- `.formula-box` - 灰色背景（公式展示）

**布局**：
- `.two-columns` - 两列网格（gap: 1.9em）
- `.comparison-box` - 对比布局
- `.comparison-left` - 左侧（红色边框）
- `.comparison-right` - 右侧（绿色边框）
- `.logo-grid` - Logo 网格（4列）
- `.logo-item` - Logo 项

**工具类**：
- `.highlight` - 黄色高亮背景
- `.checkmark` - 绿色对勾
- `.crossmark` - 红色叉号
- `.compact-list` - 紧凑列表
- `.image-caption` - 图片说明（小字、斜体、居中）

### 6.2 密度控制系统

核心提供三级密度控制，适应不同内容量：

```html
<!-- 普通密度（默认） -->
<section>
  <h2>标题</h2>
  <!-- 内容 -->
</section>

<!-- 高密度（内容较多时） -->
<section class="dense-slide">
  <h2>标题</h2>
  <!-- 更多内容 -->
</section>

<!-- 超高密度（内容非常多时） -->
<section class="ultra-dense-slide">
  <h2>标题</h2>
  <!-- 非常多的内容 -->
</section>

<!-- 宽松布局（内容较少时） -->
<section class="spacious-slide">
  <h2>标题</h2>
  <!-- 较少内容 -->
</section>
```

### 6.3 响应式单位系统

核心使用相对单位，确保任何分辨率下完美适配：

- **padding/margin**：使用 `em` 单位（随字体大小缩放）
- **spacing**：CSS 变量（`--spacing-xs` ~ `--spacing-xl`）
- **图片高度**：使用 `vh`（视口高度百分比）
- **border/radius**：使用 `em` 单位

**好处**：
- ✅ 16:9 比例下任何分辨率（720p/1080p/4K）都完美显示
- ✅ 自动缩放，无需调整
- ✅ 永不 overflow

---

## 7. 防止内容溢出的设计规范

### 7.1 核心原则（MUST FOLLOW）

#### 🔴 **RED LINE - 绝对不能超过的限制**

| 元素 | 最大值 | 说明 |
|------|--------|------|
| **图片高度** | `max-height: 39vh` | 核心默认值，超过会溢出 |
| **盒子数量** | ≤ 3个/页 | 超过3个需拆分或使用 `.dense-slide` |
| **列表项** | ≤ 8个/栏 | 两栏布局时每栏最多8项 |
| **表格行数** | ≤ 6行 | 超过需拆分 |

#### 🟡 **YELLOW LINE - 需要警惕的组合**

⚠️ **高风险组合**（很可能溢出）：
- 两栏布局 + 每栏超过2个盒子
- 大图（>30vh）+ 长文字盒子（>200字）
- 表格（>5行）+ 两栏布局
- 多个嵌套列表（3层+）

### 7.2 内容密度评估

**简单估算法**：

```
总高度 = 标题(80px) + 图片(高度+40px) + 盒子数量×120px + 列表项×30px + 表格行×40px

安全阈值：≤ 900px（屏幕） / ≤ 800px（PDF打印）
```

**使用浏览器检查**：

```javascript
// 在 Console 运行，检查溢出
document.querySelectorAll('section').forEach((section, i) => {
  const height = section.scrollHeight;
  const threshold = 900;
  if (height > threshold) {
    console.warn(`⚠️ Slide ${i+1} 可能溢出: ${height}px (阈值: ${threshold}px)`, section);
  }
});
```

### 7.3 拆分策略

#### 策略 1：图文分离

```html
<!-- 拥挤：一页包含图+文 -->
<section>
  <h2>标题</h2>
  <img src="chart.png" style="max-height: 35vh">
  <div class="observation-box">大量文字...</div>
  <ul><li>列表1</li><li>列表2</li>...</ul>
</section>

<!-- 优化：拆分为2页 -->
<section>
  <h2>标题（1/2）</h2>
  <img src="chart.png" style="max-height: 39vh">
  <p class="image-caption">图表说明</p>
</section>

<section>
  <h2>标题（2/2）</h2>
  <div class="observation-box">大量文字...</div>
  <ul><li>列表1</li><li>列表2</li>...</ul>
</section>
```

#### 策略 2：概念+细节分离

```html
<!-- 页面1：核心思想 -->
<section>
  <h2>算法原理（1/2）</h2>
  <p>核心思想简介...</p>
  <div class="solution-box">为什么需要？</div>
</section>

<!-- 页面2：技术细节 -->
<section>
  <h2>算法原理（2/2）</h2>
  <div class="two-columns">
    <div>
      <h3>公式</h3>
      <div class="formula-box">\[ E = mc^2 \]</div>
    </div>
    <div>
      <h3>优势</h3>
      <ol><li>...</li></ol>
    </div>
  </div>
</section>
```

### 7.4 使用密度控制

```html
<!-- 内容多但不想拆分？使用 dense-slide -->
<section class="dense-slide">
  <h2>密集内容</h2>
  <!-- 字体和间距会自动缩小 -->
</section>

<!-- 内容非常多？使用 ultra-dense-slide -->
<section class="ultra-dense-slide">
  <h2>超密集内容</h2>
  <!-- 进一步缩小 -->
</section>
```

---

## 8. PDF导出配置

### 8.1 导出脚本（export_pdf.sh）

项目应包含此脚本（已在核心模板中提供）：

```bash
#!/bin/bash
# export_pdf.sh

cd "$(dirname "$0")"

PORT="${PORT:-8000}"
HTML="${1:-index.html}"
OUT="${2:-out/$(basename "${HTML%.*}").pdf}"
URL="http://localhost:${PORT}/${HTML}?print-pdf"

mkdir -p "$(dirname "$OUT")"

# 启动HTTP服务器
if ! lsof -i:"${PORT}" -sTCP:LISTEN >/dev/null 2>&1; then
  nohup python3 -m http.server "${PORT}" --directory . >/dev/null 2>&1 &
  SRV_PID=$!
  CLEANUP=1
fi

# 等待服务器就绪
for i in {1..80}; do
  if curl -fsS "$URL" >/dev/null 2>&1; then break; fi
  sleep 0.1
done

# 使用Chrome导出PDF
google-chrome --headless=new --disable-gpu --no-sandbox \
    --print-to-pdf="${OUT}" \
    --print-to-pdf-no-header \
    --virtual-time-budget=20000 \
    --run-all-compositor-stages-before-draw \
    "${URL}" >/dev/null 2>&1

# 清理服务器
if [[ "${CLEANUP}" == "1" ]]; then
  kill ${SRV_PID} >/dev/null 2>&1 || true
fi

# 输出信息
if command -v pdfinfo >/dev/null 2>&1; then
  pdfinfo "${OUT}" | grep "Pages\|Page size"
fi
echo "[export] Saved -> ${OUT}"
```

### 8.2 Chrome 命令行参数说明

| 参数 | 作用 |
|------|------|
| `--headless=new` | 新版无头模式 |
| `--disable-gpu` | 禁用GPU加速（避免渲染问题） |
| `--no-sandbox` | 禁用沙箱（某些环境需要） |
| `--print-to-pdf="${OUT}"` | 指定输出文件 |
| `--print-to-pdf-no-header` | 不打印页眉页脚 |
| `--virtual-time-budget=20000` | 等待20秒加载完成（可调整） |
| `--run-all-compositor-stages-before-draw` | 确保渲染完整 |

### 8.3 打印 CSS 优化

核心框架已包含完整的打印优化（`@media print`），项目**无需额外配置**：

```css
/* 核心已提供的打印优化 */
@media print {
  /* 防止幻灯片分页 */
  .reveal .slides section {
    page-break-inside: avoid !important;
    height: auto !important;
    max-height: none !important;
    padding: 0.3em 0.6em !important;
  }

  /* 密度级别打印优化 */
  .reveal .slides section.dense-slide {
    padding: 0.4em 0.7em !important;
  }
  .reveal .slides section.ultra-dense-slide {
    padding: 0.25em 0.5em !important;
  }

  /* 防止盒子被分割 */
  .observation-box, .insight-box, .solution-box,
  .formula-box, .comparison-box, .two-columns {
    page-break-inside: avoid !important;
  }

  /* 保留颜色 */
  body {
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
  }
}
```

### 8.4 验证导出质量

```bash
# 导出PDF
./export_pdf.sh

# 检查页数（应等于幻灯片数量）
pdfinfo out/index.pdf | grep "Pages"

# 检查页面尺寸（应为 1554.96 x 875.04 pts，16:9 比例）
pdfinfo out/index.pdf | grep "Page size"

# 检查文件大小（通常 3-8MB）
ls -lh out/index.pdf
```

---

## 9. PPT 转换工具

### 9.1 使用核心提供的转换工具

核心框架提供通用的 PDF→PPT 转换工具：

```bash
# 安装依赖（仅首次）
pip install -r ../reveal-presentation-core/tools/requirements_ppt.txt

# 转换（指定输出文件名）
python3 ../reveal-presentation-core/tools/pdf_to_ppt.py out/index.pdf out/presentation.pptx

# 转换（自动命名为 index.pptx）
python3 ../reveal-presentation-core/tools/pdf_to_ppt.py out/index.pdf

# 查看帮助
python3 ../reveal-presentation-core/tools/pdf_to_ppt.py --help
```

### 9.2 转换参数

```bash
usage: pdf_to_ppt.py [-h] [--dpi DPI] pdf_file [output_file]

positional arguments:
  pdf_file     输入的PDF文件路径
  output_file  输出的PPT文件路径（可选，默认为同名.pptx）

options:
  --dpi DPI    图片DPI（默认200，越高越清晰但文件越大）
```

### 9.3 转换原理

1. 使用 `pdf2image` 将PDF每页转为高清图片（200 DPI）
2. 创建 PowerPoint 演示文稿（1920×1080 像素）
3. 每页插入对应图片，完全占满幻灯片
4. 保存为 `.pptx` 文件

**优点**：
- ✅ 完美保留样式（颜色、渐变、字体）
- ✅ 兼容所有 PowerPoint 版本
- ✅ 高清晰度（200 DPI）

---

## 10. 质量检查清单

### 10.1 开发阶段检查

#### ✅ **HTML结构**
- [ ] 所有幻灯片使用 `<section>` 标签
- [ ] 图片设置了 `max-height`（推荐 ≤ 35vh）
- [ ] 没有超长的单行文本
- [ ] 数学公式使用 MathJax 语法：`\[ ... \]` 或 `\( ... \)`
- [ ] 代码块使用 `<pre><code class="language-python">` 标签

#### ✅ **样式使用**
- [ ] 使用核心提供的样式类（`.insight-box`, `.two-columns` 等）
- [ ] **没有**创建项目特定的 `css/style.css`（除非真正需要）
- [ ] 正确引入核心样式：`../reveal-presentation-core/css/presentation-base.css`

#### ✅ **Reveal.js配置**
- [ ] `pdfMaxPagesPerSlide: 1` 已设置
- [ ] `pdfSeparateFragments: false` 已设置
- [ ] 分辨率为 1920×1080
- [ ] margin 设置为 0.08

### 10.2 导出前检查

```bash
# 1. 浏览器预览
python3 -m http.server 8000
# 打开 http://localhost:8000/index.html

# 2. 打印预览
# 访问 http://localhost:8000/index.html?print-pdf
# 检查是否有内容溢出

# 3. 检查高度（在浏览器 Console 运行）
document.querySelectorAll('section').forEach((s, i) => {
  if (s.scrollHeight > 900) console.warn(`Slide ${i+1}: ${s.scrollHeight}px`);
});
```

### 10.3 PDF导出后检查

```bash
# 检查页数
pdfinfo out/index.pdf | grep "Pages"
# 预期：幻灯片数量 ≈ PDF页数

# 检查页面尺寸
pdfinfo out/index.pdf | grep "Page size"
# 预期：1554.96 x 875.04 pts (16:9比例)

# 检查文件大小
ls -lh out/index.pdf
# 预期：3-8MB（取决于图片数量）
```

### 10.4 视觉质量检查

在PDF阅读器中逐页检查：

- [ ] 每页内容完整（无截断）
- [ ] 图片清晰（不模糊）
- [ ] 颜色正确（渐变、高亮等）
- [ ] 公式渲染正确
- [ ] 表格对齐
- [ ] 没有空白页
- [ ] 字体大小适中

---

## 11. 常见问题与解决方案

### 11.1 PDF页数多于幻灯片数

**原因**：单张幻灯片被拆分成多页

**解决方案**：
```javascript
// 确保配置中有这两行
initPresentation({
  reveal: {
    pdfMaxPagesPerSlide: 1,
    pdfSeparateFragments: false
  }
});
```

### 11.2 内容底部被截断

**原因**：内容垂直高度超过限制

**解决方案**：
1. 使用浏览器检查高度（参考 7.2 节）
2. 如果 > 900px，拆分为2页（参考 7.3 节）
3. 或者使用 `.dense-slide` / `.ultra-dense-slide`

### 11.3 公式渲染失败

**原因**：MathJax加载时间不足

**解决方案**：
```bash
# 在 export_pdf.sh 中增加等待时间
--virtual-time-budget=30000  # 改为30秒
```

### 11.4 颜色丢失

**原因**：浏览器打印时忽略背景色

**解决方案**：核心已处理，无需额外配置。如仍有问题，检查是否正确引入核心样式。

### 11.5 图片模糊

**原因**：图片原始分辨率不足

**解决方案**：
- 使用至少 1920px 宽的图片
- 或者使用矢量图（SVG）
- 避免放大小图片

### 11.6 幻灯片没有加载

**原因**：浏览器禁止 `file://` 协议访问本地文件

**解决方案**：
```bash
# 必须运行本地服务器
python3 -m http.server 8000
# 访问 http://localhost:8000/index.html
```

---

## 12. 最佳实践总结

### 12.1 设计原则

1. **一页一主题**：每张幻灯片聚焦一个核心观点
2. **图文平衡**：避免纯文字或纯图片
3. **层次分明**：使用标题、盒子、高亮区分重点
4. **留白充足**：不要填满每一寸空间
5. **颜色克制**：使用核心提供的配色系统

### 12.2 内容规划

| 幻灯片类型 | 建议内容量 | 示例 |
|-----------|-----------|------|
| **标题页** | 标题+副标题+作者 | `.title-slide` |
| **分隔页** | 章节标题 | `.section-divider` |
| **概念解释** | 定义+1-2个要点 | 单栏，`.insight-box` |
| **对比分析** | 两栏对比 | `.comparison-box` |
| **实验结果** | 图表+3-4个要点 | 图文分离 |
| **技术细节** | 公式+解释 | 拆分2页 |

### 12.3 迭代流程

```
1. 创建幻灯片 → 浏览器预览
2. 检查溢出 → 拆分/使用 .dense-slide
3. 导出PDF → 检查质量
4. 发现问题 → 调整内容或密度
5. 重新导出 → 最终验证
```

### 12.4 架构最佳实践

✅ **DO**：
- 使用核心框架的样式类
- 内容与样式分离
- 项目目录只包含 slides + images
- 所有工具使用核心提供的版本

❌ **DON'T**：
- 在项目中复制核心样式
- 创建项目特定的 CSS（除非必要）
- 硬编码配置（使用 initPresentation 传参）
- 修改核心文件（应 fork 或提 PR）

---

## 13. 工具推荐

### 13.1 开发工具

- **VS Code** + Live Server 插件
- **Chrome DevTools** - 检查元素高度、调试样式
- **Figma/Sketch** - 设计配色方案、图表

### 13.2 图片优化

```bash
# 压缩PNG（保持质量）
pngquant --quality=80-95 input.png -o output.png

# 转换为WebP（更小体积）
cwebp -q 85 input.png -o output.webp

# 批量压缩
for f in *.png; do pngquant --quality=80-95 "$f" -o "compressed_$f"; done
```

### 13.3 PDF优化

```bash
# 压缩PDF（减小文件大小）
gs -sDEVICE=pdfwrite -dCompatibilityLevel=1.4 \
   -dPDFSETTINGS=/screen \
   -dNOPAUSE -dQUIET -dBATCH \
   -sOutputFile=output_compressed.pdf input.pdf
```

### 13.4 内容溢出检测

```bash
# 使用核心提供的溢出检测工具
cd YourPresentation
npm install puppeteer  # 首次需要安装依赖

# 运行检测
node ../reveal-presentation-core/tools/overflow-detector/detect-overflow.js

# 查看报告
cat overflow-report.json
```

---

## 🎯 总结

遵循本SOP，你可以：

✅ 使用 **Reveal Presentation Core** 框架快速创建演示
✅ **零 CSS 配置**，开箱即用
✅ 完美转换为 PDF/PPT
✅ 避免内容溢出和截断
✅ 保持良好的可读性和美观性
✅ 快速迭代和调整

**核心要点**：
1. **Core-First**：优先使用核心框架，项目零 CSS
2. **Content-Only**：项目只包含内容（slides + images）
3. **配置 PDF 导出**：`pdfMaxPagesPerSlide: 1`, `pdfSeparateFragments: false`
4. **控制图片高度**：推荐 ≤ 35vh
5. **内容密集时**：使用 `.dense-slide` 或拆分
6. **使用通用工具**：所有工具在 `reveal-presentation-core/tools/`

---

**设计原则**：
1. ✅ **KISS** - Keep It Simple, Stupid
2. ✅ **DRY** - Don't Repeat Yourself (核心提供，项目复用)
3. ✅ **SoC** - Separation of Concerns (样式、逻辑、内容分离)
4. ✅ **Core-First** - 核心优先，内容纯粹

---

**版本**: 2.0
**最后更新**: 2025-11-09
**基于**: Reveal Presentation Core 架构
**作者**: Claude Code + User Collaboration

**参考示例**：查看 `WebLeaper_Presentation/` 目录了解完整的实战案例
